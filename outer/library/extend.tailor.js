define({
	
	make : function (instructions, parts) {
		console.log(instructions);
		console.log(parts);
		var tailor = document.createElement("div");
		// tailor.insertAdjacentHTML("afterbegin", "");
		return tailor;
	},

	components : {}
});	